# Optional: backend URL for API requests.
# - Leave both unset: frontend uses same-origin /api, Next.js proxies to http://127.0.0.1:8000
# - Set BACKEND_URL only: proxy target (e.g. http://127.0.0.1:8001)
# - Set NEXT_PUBLIC_API_BASE_URL: direct requests to backend (no proxy), e.g. http://localhost:8000
# BACKEND_URL=http://127.0.0.1:8000
# NEXT_PUBLIC_API_BASE_URL=
